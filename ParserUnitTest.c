
#include "Parser.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#define MAX 1024

/*int main()
{
    int i, k;
    char buf[MAX], clone[MAX];
    fgets(buf, MAX, stdin);
    printf("string is: %s\n", buf);
    printf("char 1 is : %c\n", buf[0]);
    if (buf[1] != ' ')
        printf("OK\n");
    k = 0;
    for (i = 0; i < strlen(buf) - 1; i++) {
        if (buf[i] != ' ') {
        printf("char i is : %c\n", buf[i]);
            clone[k] = buf[i];
            k++;
        }
    }

    printf("cloned: %s\n", clone);
    return 0;
}*/

